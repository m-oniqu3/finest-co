import React from "react";
import styled from "./Container.module.css";

const Container = ({ title, content, buttonText }) => {
  return <section className={styled.container}>Container</section>;
};

export default Container;
